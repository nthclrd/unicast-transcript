[project]
name = "unicast-transcriber"
version = "0.1.0"
description = "Télécharge et transcrit les podcasts Unicast (ULiège)"
requires-python = ">=3.12"
dependencies = [
    "deepgram-sdk",
    "fastapi>=0.141.1",
    "playwright",
    "python-dotenv",
    "requests",
    "uvicorn>=0.53.0",
    "yt-dlp",
]

[project.scripts]
auth       = "auth:login"
unicast    = "unicast:main"
transcribe = "transcribe:main"
